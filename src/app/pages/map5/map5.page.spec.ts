import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Map5Page } from './map5.page';

describe('Map5Page', () => {
  let component: Map5Page;
  let fixture: ComponentFixture<Map5Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Map5Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
